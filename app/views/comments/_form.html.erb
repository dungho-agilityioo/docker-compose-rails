<%= form_for Comment.new, remote: true, id: 'new_comment', url: product_comments_path(1) do |form| %>
    <%= form.text_area :content %>
    <%= form.hidden_field :product_id, :value =>1 %>
    <%= form.submit %>
<% end %>